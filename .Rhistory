spotify = read.csv("top_10000_1950-now.csv")
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
year = spotify$Album.Release.Date
year = spotify$Album.Release.Date
year
years = lapply(spotify$Album.Release.Date, function(x) substr(x, 1, 4)  )
years
hist(as.numeric(years),
breaks = "FD",
xlim = c(1950, 2030))
hist(spotify$Popularity[!spotify$Popularity == 0 ])
rm(year)
which(colnames(spotify) == "Album.Year")
colnames(spotify) == "Album.Year"
which(colnames(spotify) == "Album.Release.Date")
spotify[, -c(9, first_index:second_index)]
colnames(spotify[, -c(9, first_index:second_index)])
colnames(spotify)
spotify[, -c(which(colnames(spotify) == "Album.Release.Date"))]
spotify = spotify[, -c(which(colnames(spotify) == "Album.Release.Date"))]
sum(is.na(spotify))
lapply(spotify, function(x) sum(is.na(x)))
spotify = read.csv("top_10000_1950-now.csv")
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))]
lapply(spotify, function(x) sum(is.na(x)))
sum(is.na(spotify))
spotify = drop_na(spotify)
is.na(spotify)
spotify[is.na(spotify),]
spotify[is.na(spotify)]
spotify[is.na(spotify),]
spotify[!is.na(spotify),]
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))]
spotify = spotify[!is.na(spotify),]
years = lapply(spotify$Album.Release.Date, function(x) substr(x, 1, 4)  )
hist(as.numeric(years),
breaks = "FD",
xlim = c(1950, 2030))
rm(list=ls())
spotify = read.csv("top_10000_1950-now.csv")
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))] # delete album genres column
spotify = spotify[!is.na(spotify),] # drop the rest of the NAs
years = lapply(spotify$Album.Release.Date, function(x) substr(x, 1, 4)  )
hist(as.numeric(years),
breaks = "FD",
xlim = c(1950, 2030))
hist(spotify$Popularity[!spotify$Popularity == 0 ])
hist(as.numeric(years),
xlab = "Years From 1950 to 2030",
ylab = "Frequency",
title = "Distribution of Years of Popularity",
breaks = "FD",
xlim = c(1950, 2030))
hist(as.numeric(years),
xlab = "Years From 1950 to 2030",
ylab = "Frequency",
main = "Distribution of Years of Popularity",
breaks = "FD",
xlim = c(1950, 2030))
hist(as.numeric(years),
xlab = "Years From 1950 to 2030",
ylab = "Frequency",
main = "Distribution of Years in Popularity Variable",
breaks = "FD",
xlim = c(1950, 2030))
hist(spotify$Popularity[!spotify$Popularity == 0 ],
xlab = "Popularity Score",
ylab = "Frequency",
main = "Distribution of None-Zero Popularity Scores",
breaks = "FD")
barplot(spotify$Popularity[!spotify$Popularity == 0])
sum(spotify$Popularity[!spotify$Popularity == 0])
sum(spotify$Popularity[spotify$Popularity == 0])
spotify$Popularity[spotify$Popularity == 0]
spotify$Popularity[spotify$Popularity == 0, ]
spotify$Popularity[spotify$Popularity == 0]
spotify[spotify$Popularity == 0]
spotify[spotify$Popularity == 0,]
spotifyPopularity[spotify$Popularity == 0]
spotify$Popularity[spotify$Popularity == 0]
length(spotify$Popularity[spotify$Popularity == 0])
length(spotify$Popularity)
length(spotify$Popularity[spotify$Popularity == 0])
length(spotify$Popularity)
dim(spotify)
spotify = read.csv("top_10000_1950-now.csv")
dim(spotify)
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
dim(spotify)
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))] # delete album genres column
spotify = spotify[!is.na(spotify),] # drop the rest of the NAs
dim(spotify)
View(spotify[!is.na(spotify),] )
spotify = read.csv("top_10000_1950-now.csv")
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))] # delete album genres column
dim(spotify)
library(tidyr)
drop_na(spotify)
spotify = drop_na(spotify)
library(tidyr)
spotify = read.csv("top_10000_1950-now.csv")
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))] # delete album genres column
spotify = drop_na(spotify)
years = lapply(spotify$Album.Release.Date, function(x) substr(x, 1, 4)  )
hist(as.numeric(years),
xlab = "Years From 1950 to 2030",
ylab = "Frequency",
main = "Distribution of Years in Popularity Variable",
breaks = "FD",
xlim = c(1950, 2030))
hist(spotify$Popularity[!spotify$Popularity == 0 ],
xlab = "Popularity Score",
ylab = "Frequency",
main = "Distribution of None-Zero Popularity Scores",
breaks = "FD")
length(spotify$Popularity[spotify$Popularity == 0])
length(spotify$Popularity)
colnames(spotify)
View(spotify)
years
colnames(spotify)
spotify$Release.Year = lapply(spotify$Album.Release.Date, function(x) substr(x, 1, 4)  )
hist(as.numeric(spotify$Release.Year),
xlab = "Years From 1950 to 2030",
ylab = "Frequency",
main = "Distribution of Years in Popularity Variable",
breaks = "FD",
xlim = c(1950, 2030))
hist(spotify$Popularity[!spotify$Popularity == 0 ],
xlab = "Popularity Score",
ylab = "Frequency",
main = "Distribution of Non-Zero Popularity Scores",
breaks = "FD")
rm(list = ls())
library(tidyr)
spotify = read.csv("top_10000_1950-now.csv")
colnames(spotify)
# look for NAs for each column
lapply(spotify, function(x) sum(is.na(x)))
# find indexes of NAs in Danceability column
danceabilityNA = spotify[is.na(spotify$Danceability), ]
# since all the NAs are between danceability and time.signature columns,
# we find the column index of both so we can simply output the results
# and see if all the NAs exist in one area
first_index = which(colnames(danceabilityNA) == "Danceability")
second_index = which(colnames(danceabilityNA) == "Time.Signature")
head(danceabilityNA[, first_index:second_index])
spotify = spotify[, -c(which(colnames(spotify) == "Album.Genres"))] # delete album genres column
spotify = drop_na(spotify)
spotify$Release.Year = lapply(spotify$Album.Release.Date, function(x) substr(x, 1, 4)  )
hist(as.numeric(spotify$Release.Year),
xlab = "Years From 1950 to 2030",
ylab = "Frequency",
main = "Distribution of Years in Popularity Variable",
breaks = "FD",
xlim = c(1950, 2030))
hist(spotify$Popularity[!spotify$Popularity == 0 ],
xlab = "Popularity Score",
ylab = "Frequency",
main = "Distribution of Non-Zero Popularity Scores",
breaks = "FD")
length(spotify$Popularity[spotify$Popularity == 0])
length(spotify$Popularity)
unique(spotify$Label)
barplot(spotify$Explicit)
table(spotify$Explicit)
barplot(table(spotify$Explicit))
barplot(table(spotify$Explicit))
barplot(table(spotify$Explicit))
View(spotify)
